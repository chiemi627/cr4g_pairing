<h1>イベント作成</h1>

<div class="row">
 <div class="col-md-8 col-md-offset-2">
    <%= form_with model: Event.new, url: {action: :create}, local: true do |f| %>
    <div class="form-group">
        <%= f.label :account %>
        <div class="description">英数字16字以内で書いてください。URLに使用します。</div>
        <%= f.text_field :account, id: 'event_account' %>
    </div>
    <div class="form-group">
        <%= f.label :name %>
        <div class="description">イベント名を書いてください。日本語でもOK。</div>
        <%= f.text_field :name, id: 'event_name', size: 50 %>
    </div>
    <div class="form-group">
        <%= f.label :googlesheet %>
        <div class="description">
        Google spreadsheetのID。spreadsheetは読込可の設定にしてください。
        </div>
        <%= f.text_field :googlesheet, id: 'event_googlesheet', size:50 %>
    </div>
     <%= f.submit %>
    <% end %>
 </div>
</div>